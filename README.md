# Notes:

First node app

```
Options:
      --help     Show help                                             
      --version  Show version number                                   
  -i, --init     is the multiplier of the multiplication     
  -f, --finish   puts the limit to the table                 
  -l, --list     shows multiplication in console      
```